void emotion (float one, int two, int three, int four, int five, int six){
  if (one < 15.0) {
    Text(":-( There is icily");
    four -= 35;
    six -= 20;
  };
  if (one >= 15.0 && one <= 30.0) {
    Text("(＾• ω •＾) Meow-meow");
  };
  if (one > 30.0) {
    Text("^-^ Hot!!!");
    four -= 35;
    six -= 20;
  };
  if (two < 60) {
    Text("I'm sleepi... Z-z-z...");
    four += 15;
    six += 10;
  };
  if (three > 450){
    Text("There is loud!");
  };
  if (four == 0) {
    int DiedKittey = 1;
    while (DiedKittey == 1){
    Text("R.I.P. You killed me");
    Text("Waiting for restart");
    };
  };
  if (four < 30){
    Text("532656... Feel bad");
  };
  if (five < 30){
    Text("FEED ME!!!");
  };
  if (six < 30){
    Text("tick tac. time runs");
  };
};

